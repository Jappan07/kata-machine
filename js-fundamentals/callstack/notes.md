# Call Stack
Data structure used by javascript to keep a track of what is currently being run.
When a function starts executing it is added to the top of the call stack. When it finishes execution, it is removed from
the call stack and Js returns to whatever is on top of that call stack now.
