# Queue
FIFO

The primary methods are enqueue (adding an item to the queue),
dequeue (removing an item from the queue), and
peek (viewing the first item without removing it)


Enqueue is basically prepend
Dequeue is basically removing element from the 0th index
