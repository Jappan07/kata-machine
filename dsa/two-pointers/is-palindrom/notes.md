# Valid palindrome
maintain 2 pointers, 1 from the start of the string and one from the end
run loop till i is less than j and compare
return false when no equal is found

Everything outside the left and right pointers has already been checked and matched.

Patterns:
- Opposite-end two pointers
